# Helm chart metadata.
# version:    chart version — bump on every chart structural change
# appVersion: the application version (informational; actual image tag
#             comes from values.yaml image.tag, set by CI)
apiVersion: v2
name: eks-app
description: Production-grade Node.js order-processing API deployed on EKS
type: application
version: 0.1.0
appVersion: "0.1.0"
